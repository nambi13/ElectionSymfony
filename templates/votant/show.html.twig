{% extends 'base.html.twig' %}

{% block title %}Votant{% endblock %}

{% block body %}
    <h1>Votant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ votant.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ votant.Nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('votant_index') }}">back to list</a>

    <a href="{{ path('votant_edit', {'id': votant.id}) }}">edit</a>

    {{ include('votant/_delete_form.html.twig') }}
{% endblock %}

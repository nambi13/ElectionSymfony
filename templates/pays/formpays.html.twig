{% extends "base.html.twig" %}

{% block body %}
{% if error !="" %}
    <p>{{error}}</p>
{% endif %}

<h1>{{ form_title }}</h1>
{{ form_start(form_product, {attr: {novalidate: 'novalidate'}}) }}
    {{ form_widget(form_product) }}
{{ form_end(form_product) }}

{% endblock %}
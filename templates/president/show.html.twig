{% extends 'base.html.twig' %}

{% block title %}President{% endblock %}

{% block body %}
    <h1>President</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ president.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ president.Nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('president_index') }}">back to list</a>

    <a href="{{ path('president_edit', {'id': president.id}) }}">edit</a>

    {{ include('president/_delete_form.html.twig') }}
{% endblock %}
